# Training dependencies — install in the Colab/Kaggle notebook (GPU runtime).
# Not intended for local (Mac) installation: bitsandbytes requires CUDA.

torch>=2.2
transformers>=4.41
peft>=0.11
bitsandbytes>=0.43
trl>=0.9
accelerate>=0.30
datasets>=2.19
sentencepiece
wandb
